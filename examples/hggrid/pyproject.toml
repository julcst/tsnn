[project]
name = "hggrid"
version = "0.1.0"
description = "HGGrid: a hierarchical grid of truncated Gaussians for 2-D density estimation"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.6",
    "slangpy>=0.42.0,<0.43",
    "tqdm>=4.67.3",
]
